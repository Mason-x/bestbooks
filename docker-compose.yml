services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: bestbooks-app:latest
    container_name: bestbooks-app
    restart: unless-stopped
    ports:
      - "${APP_PORT:-3000}:3000"
    environment:
      NODE_ENV: production
      PORT: 3000
      DATABASE_URL: "${DATABASE_URL}"
